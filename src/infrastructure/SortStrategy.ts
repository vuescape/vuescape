/** Represents a Sort Strategy to use when sorting. */
export enum SortStrategy {
  /** Sort using default comparison  */
  Default = 0,
  /** Sort string using case insensitive  sort comparison. */
  StringCaseInsensitive = 1,
}
